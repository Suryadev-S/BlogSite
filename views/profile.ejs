<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm" crossorigin="anonymous" defer></script>
    <link rel="stylesheet" href="style.css">
    <script src="script.js" defer></script>
</head>
<body>
    <%-include('./nav')%>
    <div class="container mt-5">
        <h1>Profile</h1>
        <table class="table table-borderless">
            <tr>
                <th>Username</th>
                <td><%=user.username%></td>
            </tr>
            <tr>
                <th>Password</th>
                <td><%=user.password%></td>
            </tr>
            <tr>
                <th>Id</th>
                <td><%=user.id%></td>
            </tr>
        </table>
        <div class="container-fluid mt-5">
            <h3>Blogs written</h3>
            <%if(allBlogs.length){%>
                <%allBlogs.forEach((blog)=>{%>
                    <div>
                        <h3><a href="/selected?id=<%=blog._id%>"><%=blog.title%></a></h3>
                        <p><%=blog.desc%></p>
                        <small><%=blog.author%></small>
                        <div class="d-flex w-25 justify-content-between">
                            <p><%=blog._id%></p>
                            <a href="/edit?id=<%=blog._id%>" class="btn btn-primary">edit</a>
                            <form action="/delete?id=<%=blog._id%>" method="POST">
                                <button class="btn btn-primary" type="submit" id="deleteButton">Delete</button>
                            </form>
                        </div>
                    </div>
                <%});%>
            <%}else{%>
                <p>No blogs as of now. Login to create</p>
            <%}%>
        </div>
    </div>
</body>
</html>